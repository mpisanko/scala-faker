import org.scalatest._

class FakerSpec extends FlatSpec with Matchers {

  // it should "fake a sentence from a given input no longer than input" in {
  //   val words = "I wish I may I wish I might".split(" ").toStream
  //   val faker = new Faker(words)
  //   faker.fake.size should be <= words.size
  // }
}